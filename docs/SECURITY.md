# Security (Fabric doc alias)

This is a placeholder document for the Fabric docs alias `security`.

- Configure `fabric.docs_root` to point at a directory containing your Fabric docs.
- Then run: `fabric-open-doc security`

For VANTAGE’s Fabric integration security model, see:
- `FABRIC_INTEGRATION.md`

